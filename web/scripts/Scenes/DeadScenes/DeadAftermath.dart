import "dart:html";
import "../../SBURBSim.dart";
///most dead sessions end in, well, death.  if they succede, print out a link to a regular session where they are added.
///or just start up a enw session within this page like a combo session. whatever is easier when i get there.
///
///
class DeadAftermath extends Scene {

    DeadAftermath(Session session) : super(session);

@override
void renderContent(Element div) {
    // TODO: implement renderContent
}

@override
bool trigger(List<Player> playerList) {
    // TODO: implement trigger
}
}