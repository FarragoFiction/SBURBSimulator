// OK, so this file used to be the main controller but due to some unknown black magic it didn't work with dartdevc
// all the actual controller code now lives over in SimIndexController.dart, which makes it work
// don't know why but honestly at this point I don't even care
// look into the issue if you dare - PL
import '../../SBURBSim.dart';
class StoryController extends SimController {
  StoryController() : super();
}